# Workaround: @crayonai/react-core@0.7.7 declares peer deps on
# tailwind-merge@^2.5.4 and zustand@^4.5.5, but the project uses v3/v5.
# Remove this once crayonai updates their peer dependency ranges.
legacy-peer-deps=true
