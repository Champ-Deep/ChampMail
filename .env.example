# ChampMail Environment Configuration
# Copy this file to .env and configure for your environment

# ============================================
# Application Settings
# ============================================
DEBUG=false
ENVIRONMENT=production

# Ports (change if conflicts)
FRONTEND_PORT=3000
BACKEND_PORT=8000
POSTGRES_PORT=5432
FALKORDB_PORT=6379
FALKORDB_BROWSER_PORT=3001
REDIS_PORT=6380

# ============================================
# Database Credentials
# ============================================
# PostgreSQL (User management)
POSTGRES_USER=champmail
POSTGRES_PASSWORD=change_this_password
POSTGRES_DB=champmail

# FalkorDB (Graph database) - leave empty for no password
FALKORDB_PASSWORD=

# Redis (Caching) - leave empty for no password
REDIS_PASSWORD=

# ============================================
# Security
# ============================================
# Generate a secure key: openssl rand -hex 32
JWT_SECRET_KEY=change-this-to-a-long-random-string-at-least-32-chars

# Email credential encryption key (auto-generated if empty)
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
EMAIL_ENCRYPTION_KEY=

# ============================================
# AI Features (Optional)
# ============================================
# OpenAI API key for AI-powered features
OPENAI_API_KEY=

# ============================================
# n8n Integration
# ============================================
# n8n webhook base URL for email automation workflows
N8N_WEBHOOK_URL=http://localhost:5678/webhook
# n8n API key for authentication (optional)
N8N_API_KEY=

# ============================================
# SMTP/IMAP (Configure in UI after login)
# ============================================
# These are set per-user in the Settings page
# Default values for development only:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# IMAP_USERNAME=
# IMAP_PASSWORD=
